import sys
input = sys.stdin.readline
n = int(input())

# 재귀함수
def factorial(x):
    if x <= 1:
        return 1
    return x * factorial(x-1)

print(factorial(n))

# 반복문
total = 1
for i in range(1,N+1):
    total *= i

print(total)
